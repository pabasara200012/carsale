# Temporary Firestore Security Rules for Development
# WARNING: These rules allow anyone to read/write to your database. 
# Use only for development and testing!

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

# To apply these rules:
# 1. Go to Firebase Console: https://console.firebase.google.com/
# 2. Select your project: carsale-5904d
# 3. Go to Firestore Database
# 4. Click on "Rules" tab
# 5. Replace the current rules with the above rules
# 6. Click "Publish"

# After development, replace with the production rules from firestore.rules file
